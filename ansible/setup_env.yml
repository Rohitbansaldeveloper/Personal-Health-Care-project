---
- name: Prepare Deployment Environment
  hosts: localhost
  tasks:
    - name: Ensure Docker is installed
      apt:
        name: docker.io
        state: present
    - name: Create directory for Patient Reports
      file:
        path: /var/lib/healthcare/reports
        state: directory
        mode: '0755'
